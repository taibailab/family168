CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE CHILD(ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,NAME VARCHAR(255),PARENT_ID BIGINT)
CREATE MEMORY TABLE PARENT(ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,NAME VARCHAR(255))
CREATE MEMORY TABLE USER(ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,NAME VARCHAR(255))
ALTER TABLE CHILD ADD CONSTRAINT FK3E104FCD36569B8 FOREIGN KEY(PARENT_ID) REFERENCES PARENT(ID)
ALTER TABLE CHILD ALTER COLUMN ID RESTART WITH 6
ALTER TABLE PARENT ALTER COLUMN ID RESTART WITH 1
ALTER TABLE USER ALTER COLUMN ID RESTART WITH 1
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 10
SET SCHEMA PUBLIC
INSERT INTO CHILD VALUES(1,'child',NULL)
INSERT INTO CHILD VALUES(2,'boy',NULL)
INSERT INTO CHILD VALUES(3,'girl',NULL)
INSERT INTO CHILD VALUES(4,'other boy',NULL)
INSERT INTO CHILD VALUES(5,'other girl',NULL)
